import { createApp } from 'vue'
import App from './App.vue'

// Create and mount the application
createApp(App).mount('#app')

// Log that the examples app is running
console.log('LetterPeople Vue examples running')